{
  "scripts": {
    "test": "yarn -s run lint",
    "lint": "yarn -s run lint:prettier",
    "lint:prettier": "prettier . --check",
    "fix": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.4.1"
  }
}
